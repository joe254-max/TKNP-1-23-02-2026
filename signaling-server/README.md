Signaling server (Socket.io)

Install and run:

```bash
cd signaling-server
npm install
node index.js
```

Configure your front-end to point `VITE_SIGNALING_SERVER_URL` to the server (e.g. http://your-server:4000).